{
  "name": "agent-flow",
  "private": true,
  "version": "0.1.0",
  "description": "AI Agent Workflow Orchestrator - 总控平台",
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "packages/client",
    "packages/server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "npm run dev -w packages/client",
    "dev:server": "npm run dev -w packages/server",
    "build": "npm run build -w packages/client && npm run build -w packages/server",
    "predeploy": "npm run build -w packages/client && cp packages/client/dist/index.html packages/client/dist/404.html",
    "deploy": "gh-pages -d packages/client/dist",
    "sync-context": "tsx scripts/sync-context.ts"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "gh-pages": "^6.3.0",
    "typescript": "~6.0.2"
  }
}
