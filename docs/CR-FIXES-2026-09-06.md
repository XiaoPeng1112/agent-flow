# 全量 CR 修复记录 — 2026-09-06

对应 `CODE-REVIEW-2026-09-06.md` 的 13 项确认问题，以下记录修复后的行为。原审查报告保留为历史证据，其中行号、代码片段及复现输出代表修复前状态。

按当前单机使用场景验收。同步仅保留数据保护修复，通过本地模拟远端验证，无需第二台设备或远端配置。

| 原编号 | 修复后的行为 |
| --- | --- |
| 1 | 缺失记录不再代表删除；明确删除保存持久化 tombstone。同步基于内容版本与共同基线识别冲突，远端 CAS 失败不再刷新 SHA 后覆盖。 |
| 2 | 重置、回滚、跳过和删除先停止相关执行及验证，再修改状态；旧执行回调不能覆盖新状态，异步失败有终结处理。 |
| 3 | 回滚记录新的尝试起点；重试只继承当前尝试及当前上游版本，避免恢复旧代码。 |
| 4 | 沿有效条件边穿过跳过节点，收集已完成祖先的代码快照；模板实例化保留边条件。 |
| 5 | SQLite 保存节点产物前删除旧集合；回滚清空的产物不会在重载后复活。 |
| 6 | 自动提取的 Skill 写入应用数据中的项目专属目录，并加入发现路径；原项目目录保持可交付，旧位置仍可读取。 |
| 7 | 打回回到 ready，由调度器按容量启动真实执行；前端同步修改建议。 |
| 8 | 验证脚本使用受管理的 worker，取消/超时清理普通子进程；遗留后台进程不能判定验证成功。 |
| 9 | 验证列表包含尚未验证或证据过期的节点，保留重新验证入口及错误提示。 |
| 10 | 合并前驱快照后在隔离工作区解析 scriptCwd；准备失败的工作区也保留执行记录。 |
| 11 | 审核反馈独立保存并传给下游，不再通过修改产物使已通过的验证证据失效。 |
| 12 | 同步版本覆盖节点、Turn 等实际内容变化，不再依赖 createdAt/startedAt 判断新旧。 |
| 13 | REST 列表以请求时版本校验，拒绝覆盖更新的 WebSocket 状态，并保留其他项目任务；被丢弃的请求会重新加载，持续变化时明确报错。回滚广播完整 Run，节点更新携带完整节点。 |

## 验证

新增同步协调、工作流跨模块、前端状态与验证恢复 3 组回归文件，共 24 项测试。使用临时 Git 仓库、测试 SQLite、真实本机 worker，以及模拟远端 API；覆盖执行中重置/回滚/删除、旧回调、v1 回滚至 v2、跳过节点、条件边、多前驱生成工作目录、产物重载、打回容量、反馈证据、Skill 提取后的实际合入、验证后台进程和取消、验证缓存恢复、REST/WebSocket 竞争及重新加载。

集成测试会启动多个真实进程，测试文件并发上限设为 4，启动等待明确设置上限，避免与构建争用本机资源造成偶发超时。

最终检查结果见 `UPGRADE-PROGRESS.md` 的第五批记录。

## 验证边界

未调用真实模型，未访问真实同步远端，未启动项目应用迁移个人数据库。没有提交或推送工作区改动。同步测试使用同机模拟的独立设备状态；未来启用多设备时应先统一更新客户端，旧版本不具备本次 tombstone 与冲突保护逻辑。

进程清理在 macOS/POSIX 验证；主动脱离进程组的 daemon 仍需要操作系统级隔离。全局执行配额、持久化事件事务及工作区保留策略属于后续优化，不计入本次 13 项 CR 缺陷修复。
